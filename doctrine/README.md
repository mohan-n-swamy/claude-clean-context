# Doctrine

`CLAUDE.md.template` is a starter doctrine for agent-assisted engineering. Copy it into a global or project-level instruction file, trim it to your workflow, and keep the parts that must shape every session. The hooks in [../hooks](../hooks) are the enforcement layer: doctrine states the rule; hooks make the rule deterministic.
